---
description: D2C 设计稿到代码的组件映射规则与 AI 生成策略（借鉴去哪儿"规则+AI"融合架构）
globs: apps/miniapp-client/**/*.vue
alwaysApply: false
---

# D2C 设计稿到代码映射规则

当根据设计稿（截图、Figma 链接、设计描述）生成小程序页面代码时，遵循以下规则。
核心理念：**规则保精准（Token/间距/尺寸），AI 管语义（组件拆分/代码结构/工程化）**。

---

## 1. 组件映射表（设计元素 → wot-design-uni 组件）

| 设计元素 | 推荐组件 | 备注 |
|----------|---------|------|
| 普通按钮 | `<wd-button>` | type="primary" 为品牌色，round 为胶囊形 |
| 图标按钮 | `<wd-icon>` + 点击处理 | 最小热区 88rpx |
| 搜索框 | `<wd-search>` | 胶囊形，高度 64rpx |
| 弹出层 | `<wd-popup>` | 底部弹出用 position="bottom" |
| 动作面板 | `<wd-action-sheet>` | — |
| 对话框 | `<wd-message-box>` | — |
| Toast 提示 | `<wd-toast>` 或 `uni.showToast` | 1500ms |
| 轻提示 | `<wd-notify>` | — |
| 标签页 | `<wd-tabs>` | — |
| 步进器 | `<wd-input-number>` | 购物车数量选择 |
| 单选/多选 | `<wd-radio>` / `<wd-checkbox>` | — |
| 开关 | `<wd-switch>` | — |
| 表单 | `<wd-form>` + `<wd-cell>` | — |
| 图片预览 | `<wd-img-preview>` | — |
| 骨架屏 | `<wd-skeleton>` | 加载状态必用 |
| 下拉刷新/列表 | `<z-paging>` | 长列表标配 |
| 空状态 | `<wd-status-tip>` | — |
| 标签 | `<wd-tag>` | — |
| 价格 | 自定义 PriceDisplay 组件 | 见 §3 |
| 商品卡片 | 自定义 ProductCard 组件 | 见 miniapp-ui-spec.mdc §2.1 |
| 导航栏 | `<wd-navbar>` 或自定义 | 沉浸式需自定义 |
| 底部 tabbar | 自定义 tabbar（已有） | `src/tabbar/` |

**规则**：能用 wot-design-uni 组件的不自己写 div 堆叠。自定义组件仅在 wot-design-uni 无法满足时使用。

---

## 2. 页面结构生成策略

### 2.1 分步生成（先 UI 后逻辑）

参考去哪儿的分步出码策略，生成页面时分两步：

**第一步：UI 结构**
- 根据设计稿/截图生成纯 UI 代码
- 使用 Design Token（颜色、间距、字号、圆角）
- 正确的组件映射
- 数据使用静态 mock

**第二步：逻辑注入**
- 替换 mock 数据为 API 调用（`src/api/` 中的接口）
- 接入 Pinia store（`src/store/`）
- 添加交互逻辑（点击、跳转、加购等）
- 处理加载/错误/空状态

### 2.2 组件拆分规则

按**功能边界**拆分，不按视觉区域拆分：

```
pages/product/detail.vue          ← 页面容器
  └─ components/
     ├─ product-gallery.vue       ← 商品图片轮播
     ├─ product-info.vue          ← 基本信息（标题+价格+标签）
     ├─ product-specs.vue         ← 规格选择
     ├─ product-reviews.vue       ← 评价模块
     └─ product-cart-bar.vue      ← 底部购物车栏
```

**拆分原则**：
- 可独立复用的模块必须拆分为独立组件
- 同一业务实体的展示聚合在一个组件内
- 数据通过 Props 传入，事件通过 Emits 传出
- 页面组件放 `pages/xx/components/`，跨页面复用的放 `components/`

### 2.3 数据与 UI 解耦

```vue
<!-- 正确：数据驱动 -->
<ProductCard v-for="item in products" :key="item.id" :product="item" @add-cart="onAddCart" />

<!-- 错误：数据写死在模板中 -->
<view>
  <text>有机牛奶</text>
  <text>¥18.90</text>
</view>
```

---

## 3. 样式转换规则

### 3.1 设计稿尺寸 → 代码单位

| 设计稿 (750px 基准) | 代码 | 示例 |
|---------------------|------|------|
| 32px | 32rpx 或 Design Token | `--space-lg` |
| 16px | 16rpx | `--space-sm` |
| 8px | 8rpx | `--space-xs` |
| 1px 边框 | 1px | 直接 1px |

### 3.2 颜色转换

设计稿中的色值必须映射到最近的 Design Token：

| 设计稿色值 | CSS 变量 | UnoCSS 类 |
|-----------|---------|-----------|
| #00C250 / 绿色系 | `--color-brand-primary` | `text-primary` / `bg-primary` |
| #FF3B30 / 红色价格 | `--color-price` | `text-price` |
| #FF4D4F / 红色警示 | `--color-func-error` | `text-error` |
| #111111 / 黑色主文字 | `--color-text-primary` | `text-ink` |
| #666666 / 灰色副文字 | `--color-text-secondary` | `text-ink-light` |
| #999999 / 浅灰文字 | `--color-text-tertiary` | `text-ink-lighter` |
| #F5F5F5 / 浅灰背景 | `--color-bg-body` | `bg-fill` |
| #FFFFFF / 白色卡片 | `--color-bg-surface` | `bg-surface` |

如果设计稿色值不在 Token 中，优先找最近的 Token；确实需要新增再讨论。

### 3.3 布局转换

| 设计稿布局 | 代码实现 |
|-----------|---------|
| 水平排列等间距 | `flex justify-between` 或 `flex gap-space-sm` |
| 垂直居中 | `flex items-center` |
| 水平垂直居中 | `center`（已定义 shortcut） |
| 两列网格 | `grid grid-cols-2 gap-space-sm` |
| 左图右文 | `flex gap-space-md` + 固定宽图片 |
| 固定底部 | `fixed bottom-0 left-0 right-0` + safe-area |

---

## 4. 资源处理

| 资源类型 | 处理方式 |
|---------|---------|
| 商品图片 | CDN URL，`<image>` + `lazy-load` + `mode="aspectFill"` |
| 图标 | UnoCSS Icons（`i-my-icons-xxx`）或 iconfont |
| 装饰图片 | CDN，不打包进小程序 |
| SVG | 通过 `@iconify` 注册为 UnoCSS 图标集 |

---

## 5. 生成代码模板

### 5.1 标准页面模板

```vue
<route lang="json">
{ "style": { "navigationBarTitleText": "页面标题" } }
</route>

<script setup lang="ts">
import { ref } from 'vue'

interface PageItem {
  id: number
  title: string
  price: number
  image: string
}

const list = ref<PageItem[]>([])
const loading = ref(true)
</script>

<template>
  <view class="min-h-screen bg-fill">
    <wd-skeleton v-if="loading" :row="3" />
    <view v-else class="p-space-lg">
      <!-- 内容 -->
    </view>
  </view>
</template>

<style lang="scss" scoped>
// 仅写 Design Token 无法覆盖的特殊样式
</style>
```

### 5.2 标准组件模板

```vue
<script setup lang="ts">
interface Props {
  title: string
  price: number
  image: string
}

const props = defineProps<Props>()
const emit = defineEmits<{
  click: []
  addCart: []
}>()
</script>

<template>
  <view class="bg-surface rounded-card shadow-card" @tap="emit('click')">
    <slot />
  </view>
</template>

<style lang="scss" scoped>
</style>
```

---

## 6. 禁止事项

- **禁止** 生成"div 海"：使用语义化的 `<view>`/`<text>`/`<image>`/`<button>` + wot-design-uni 组件
- **禁止** 硬编码色值、字号、间距
- **禁止** 使用 spinner/菊花图加载（用骨架屏）
- **禁止** 忽略安全区域（底部固定元素必须处理 safe-area）
- **禁止** 一个文件超过 300 行（必须拆分组件）
- **禁止** 在模板中写复杂逻辑（提取到 `<script setup>` 的 computed/方法中）
