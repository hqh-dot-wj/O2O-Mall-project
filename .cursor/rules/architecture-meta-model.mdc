---
description: 架构决策元模型——稳定的原则层（相态感知、四棱镜、结构不变量、认知陷阱）
globs: docs/design/**,docs/requirements/**,**/*.module.ts,apps/backend/src/module/**/*.ts
alwaysApply: false
---

# 架构决策元模型（Architecture Decision Meta-Model）

> 架构设计和重构时使用。引用方式：`#architecture` 或 @architecture-meta-model
>
> 本文件只包含**可复用的原则**，不含项目特定数据。
> 项目当前相态、Owner、Review 节奏 → 见 `architecture-playbook.mdc`
> PR 可直接复制的决策模板 → 见 `architecture-checklist.mdc`

## 核心原则

不依赖"我见过类似问题"的经验匹配，而是通过以下元结构推导出当前问题的最优解。

---

## 0. 触发条件——什么时候需要走架构决策流程？

不是所有改动都需要走决策流程。以下任一条件命中，则必须启动：

| 触发条件 | 对应决策级别 | 说明 |
| -------- | ------------ | ---- |
| 新增或替换外部依赖（npm 包、云服务、中间件） | L2+ | 引入即有退出成本 |
| 新增跨模块依赖（Module A → Module B） | L2+ | 改变系统连接拓扑 |
| 数据模型变更（新增表、改字段类型、改关联关系） | L2+ | 影响面广，难回退 |
| 新增应用或共享库（`apps/*`、`libs/*`） | L3+ | 改变仓库结构 |
| 技术选型（框架、语言、协议、存储引擎） | L3+ | 长期锁定风险 |
| 架构重构（模块拆分/合并、分层调整） | L3+ | 影响全局结构 |
| 影响多个应用（backend + admin-web + miniapp） | L4 | 需要跨团队协调 |

**未命中任何条件**：按正常开发流程即可，无需额外决策分析。

---

## 1. 相态感知（Phase Awareness）

所有决策规则的权重随系统所处的"相态"动态调整。
在应用四棱镜之前，必须先判断涉及模块当前处于哪个相态。

### 1.1 三种相态

| 相态   | 特征 | 核心目标 | 架构的角色 |
| ------ | ---- | -------- | ---------- |
| 探索态 | PMF 未验证、用户 < 1000、商业模式假设阶段 | 最快速度验证假设 | 不阻碍速度，保持可抛弃性 |
| 成长态 | PMF 已验证、用户快速增长、团队扩张 | 速度与可持续性平衡 | 建立关键抽象层，管理复杂性增长 |
| 运营态 | 产品成熟、用户规模稳定、收入可预测 | 可靠性、效率、长期可维护 | 系统性管理复杂性，优化长期成本 |

### 1.2 量化参考标准

| 指标 | 探索态 | 成长态 | 运营态 |
| ---- | ------ | ------ | ------ |
| 模块变更频率 | > 10 次/月 | 3-10 次/月 | < 3 次/月 |
| 需求确定性 | < 50% 需求会保留 | 50-80% | > 80% |
| 核心模型变更 | 每 1-2 周 | 每 1-3 月 | 每季度或更少 |
| 用户/流量增长率 | 不确定/剧烈波动 | 月增 > 20% | 月增 < 5% |
| 团队熟悉度 | 1-2 人了解 | 3-5 人了解 | 团队普遍了解 |

### 1.3 相态如何影响四棱镜权重

| 棱镜 | 探索态 | 成长态 | 运营态 |
| ---- | ------ | ------ | ------ |
| 时间棱镜 | 几乎只看短期 | 短期与中期并重 | 三维度均衡 |
| 能力棱镜 | 放宽（允许单点依赖） | 收紧（消除单点） | 严格执行 |
| 复杂性棱镜 | 容忍（可抛弃即可） | 偿还 + 建立抽象层 | 严格控制 |
| 业务价值棱镜 | 最高权重 | 高权重（ROI 必须明确） | 中等权重 |

### 1.4 相态判断的元规则

- 不要把阶段性策略当成普适原则。"快速试错"是探索态的最优策略，不是所有阶段的。
- 相态转换有滞后性——团队心智往往滞后于产品实际相态。
- 同一系统的不同模块可能处于不同相态，**按模块判断，不要一刀切**。

---

## 2. 决策四棱镜

每个架构决策必须同时通过四个棱镜审视。

### 2.1 时间棱镜：半衰期

| 维度 | 问题 | 判断标准 |
| ---- | ---- | -------- |
| 短期收益 | 本季度能解决什么？ | 明确、可量化 |
| 中期成本 | 1 年后维护成本？ | 团队能否独立排障 |
| 长期锁定 | 3 年后能否低成本替换？ | 是否有清晰抽象边界 |

**收益/成本比的最低要求**：探索态 ≥ 0.5x → 成长态 ≥ 2x → 运营态 ≥ 3x。

### 2.2 能力棱镜：团队可达边界

| 维度 | 问题 | 红线 |
| ---- | ---- | ---- |
| 认知负担 | 从"会用"到"能排查深度故障"要多久？ | > 3 个月则高风险 |
| 单点风险 | 唯一精通者离职怎么办？ | 无预案则禁止引入 |
| 能力拉伸 | 是舒适区边缘拉伸，还是未知跳跃？ | 跳跃则需降级方案 |

**元规则**：技术选择必须在团队**可达边界**内，而非"理论最优"。

### 2.3 复杂性棱镜：注入成本

| 成本类型 | 具体表现 | 评估方式 |
| -------- | -------- | -------- |
| 显性成本 | 服务器、许可、运维 | 可直接计算 |
| 隐性成本 | 编译时间、部署复杂度、新人上手门槛 | 需要估算 |
| 传染成本 | 复杂性是否会扩散到其他模块？ | 依赖分析 |

**元规则**：每引入一个新技术组件，必须同时回答"它的**退出成本**是多少"。

### 2.4 业务价值棱镜：真实贡献

| 维度 | 问题 | 判断标准 |
| ---- | ---- | -------- |
| 价值链位置 | 核心链路还是辅助链路？ | 核心链路权重更高 |
| 技术 ROI | 投入 X 人天，预期带来什么？ | 无法量化则降低优先级 |
| 用户感知 | 用户能直接感知改善吗？ | 无感知的优化需要更强理由 |
| 机会成本 | 做这件事 = 不做什么？ | 机会成本 > 直接收益则重新评估 |

### 2.5 棱镜冲突裁决规则

当四个棱镜的结论相互矛盾时，按以下优先级裁决：

| 优先级 | 规则 | 说明 |
| ------ | ---- | ---- |
| **P0** | 红线一票否决 | 能力棱镜的红线（无预案的单点风险、认知负担 > 3 月）触发则直接否决，不论其他棱镜多好 |
| **P1** | 相态权重优先 | 按 §1.3 的相态权重，权重最高的棱镜结论优先（如探索态下业务价值棱镜权重最高） |
| **P2** | 可逆性打破平票 | 剩余棱镜仍打平时，选择**退出成本更低**的方案（双向门优于单向门） |
| **P3** | 降级方案兜底 | 如果最优方案在某个棱镜上不过关，但有可行降级方案，可附条件通过 |

### 2.6 约束波动性（AI 时代的元约束）

- 对 AI 能力相关的决策，半衰期按**月**而非按年评估。
- 依赖特定 AI 模型能力的设计，必须有"模型可插拔"的抽象层。
- 区分"稳定约束"（用户需要什么）和"易变约束"（用什么技术实现），架构锚点在稳定约束上。
- 对 AI 密集型模块，定期评估"这个模块是否还需要存在"。

---

## 3. 约束发现——决策前的必做步骤

在构思方案之前，先穷举约束，避免"先想方案再发现约束不满足"的返工。

### 3.1 约束分类清单

| 约束类型 | 典型问题 | 示例 |
| -------- | -------- | ---- |
| 时间约束 | 什么时候必须交付？有没有硬性截止日期？ | "下周上线活动页" |
| 人力约束 | 谁来做？能投入多少人天？ | "只有 1 人熟悉该模块" |
| 技术约束 | 现有技术栈、基础设施的限制？ | "生产环境没有 Redis Cluster" |
| 兼容约束 | 需要兼容哪些已有系统/接口/数据？ | "v1 API 不能破坏" |
| 合规约束 | 有没有安全/隐私/审计方面的硬性要求？ | "用户数据不能出境" |
| 依赖约束 | 上下游系统/团队的交付节奏？ | "支付网关 3 月才能上线" |

### 3.2 约束发现的元规则

- 约束先于方案：先列约束，再在约束空间内寻找方案。
- 区分硬约束（不可违反）和软约束（可协商/降级）。
- 隐性约束比显性约束更危险——主动追问"还有什么我没考虑到的"。

---

## 4. 结构元模型（系统设计的不变量）

### 4.1 边界先于结构

在设计内部结构之前，先回答："这个模块/服务**绝对不负责**什么？"

- 核心职责在 3 句话内说清。
- 排除项在 5 句话内说清。
- 两个都说不清 → 边界不够清晰，不要开始写代码。
- 两个模块的"负责"清单有交集 → 必须明确归属或提取独立模块。

### 4.2 连接比组件更贵

系统中最昂贵的不是组件本身，而是组件之间的连接。

- **最小化**连接数：能不依赖就不依赖。
- **稳固**必要连接：必须存在的连接要有契约（接口、类型、版本）。
- **可见化**连接：所有跨模块调用必须可追踪。

### 4.3 抽象层稳定性判断

| 信号 | 含义 |
| ---- | ---- |
| 抽象层接口在 3 次迭代中未变 | 稳定，继续保持 |
| 抽象层接口每次迭代都要改 | 抽象错误，需要重新设计 |
| 抽象层被绕过（直接访问底层） | 抽象不够用，需要扩展或替换 |

---

## 5. 认知陷阱（Anti-Patterns）

| 陷阱 | 表现 | 纠正 |
| ---- | ---- | ---- |
| 幸存者偏差 | "XX 公司这么做成功了" | 成功做法 ≠ 因果；回到四棱镜评估自己的约束 |
| 阶段错配 | 产品已进成长态，团队还用探索态方式 | 用相态判断校准，按模块判断 |
| 反向阶段错配 | 探索态却用运营态标准做架构 | 探索态代码允许"可抛弃" |
| 速度崇拜 | 把"快"本身当目标 | 速度的价值在于缩短反馈环，不在于产出更多代码 |
| 完美主义 | 探索态追求完美架构 | 质量标准是"能验证假设"，不是"能承受 10 倍流量" |
| 教条主义 | 把方法论当不可质疑的真理 | 元模型本身也需要迭代；推导出明显不合理的结论时质疑棱镜 |
| 技术自嗨 | "技术上很优雅"但业务无感知 | 无法量化业务收益的技术优化，降低优先级 |
| 过度共识 | 反复开会，决策瘫痪 | 用决策分级明确谁有权拍板 |
| 孤岛决策 | 少数人决策，执行时团队不配合 | L3/L4 必须有 Design Doc 或评审记录 |

---

## 6. 元模型自身的迭代规则

- **迭代频率**：每半年 Review 一次。
- **迭代信号**：
  - 四棱镜推导出明显不合理的结论 → 检查棱镜定义。
  - 决策模板某板块长期填"N/A" → 考虑移除或合并。
  - 团队反馈"太重用不起来" → 调整分级或简化。
  - 新的认知陷阱被发现 → 补充到 §5。
- **迭代方式**：提 PR 修改本文件，PR 描述说明修改原因和预期效果。
