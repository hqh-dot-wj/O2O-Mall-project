---
description: 需求文档与设计文档规范（必含 7 类图、11/14 章节结构、文档目录归类、缺陷分析前置检查、大模块先整体后子模块）
globs: docs/**/*.md, **/docs/**/*.md, apps/backend/docs/**/*.md
alwaysApply: false
---

# 需求文档与设计文档规范

生成或编写**需求文档**、**设计文档**时遵循本规范。适用于：新功能规划、接口设计、页面设计、模块实现等需正式文档产出的场景。

---

## 1. 文档类型与产出要求

| 文档类型     | 说明                                 | 必含内容                                                     |
| ------------ | ------------------------------------ | ------------------------------------------------------------ |
| **需求文档** | 定义功能目标、用户故事、验收标准     | 用例图、活动图、状态图（若涉及状态流转）                     |
| **设计文档** | 定义技术方案、模块划分、数据流、部署 | 类图、时序图、组件图、部署图；若业务复杂，补充状态图、活动图 |

---

## 2. 必含的 7 类图

| 图类型     | 用途                      | 适用文档  | 典型场景                         |
| ---------- | ------------------------- | --------- | -------------------------------- |
| **用例图** | 角色与用例关系、系统边界  | 需求文档  | 功能范围、谁用什么功能           |
| **类图**   | 实体、DTO、领域模型及关系 | 设计文档  | 数据结构、模块依赖               |
| **时序图** | 调用顺序、消息流          | 设计文档  | API 调用、前后端交互、服务间调用 |
| **组件图** | 模块/包划分与依赖         | 设计文档  | 架构分层、包结构                 |
| **部署图** | 运行环境、节点、部署关系  | 设计文档  | 服务部署、环境拓扑               |
| **状态图** | 状态及转换条件            | 需求/设计 | 订单状态、审批流、业务状态机     |
| **活动图** | 流程、分支、并行          | 需求文档  | 业务流程、操作步骤               |

**选图原则**：不强行凑图；若某图对当前场景无实际帮助，可省略并**在文档中说明省略原因**。

---

## 3. 文字描述规范（精准）

- **术语统一**：同一概念全文使用同一术语，避免混用（如「用户」与「会员」二选一）。
- **主谓宾完整**：每句有明确主语、谓语、宾语，避免模糊表述。
- **量化优先**：能用数字的用数字（如「3 秒内」「最多 100 条」），避免「较快」「较多」。
- **禁止歧义**：如「支持 A 和 B 的 C」需明确是「(A 和 B) 的 C」还是「A 和 (B 的 C)」。
- **引用准确**：引用代码、接口、表名时使用准确标识（如 `GET /api/xxx`、`sys_user`）。

---

## 4. 逻辑严谨性要求

- **因果清晰**：先因后果，条件与结论一一对应；若存在多分支，穷举或说明「其它情况」。
- **前后一致**：需求与设计、图与文字、各章节之间不得矛盾。
- **边界明确**：说明前置条件、后置条件、异常路径、超时/失败处理。
- **可验证**：验收标准可操作、可测试，避免「体验良好」「性能优秀」等主观描述。
- **层级分明**：先总体后局部，先概念后细节；章节编号与层级清晰。

---

## 5. 文档结构标准（必须遵守）

### 5.1 需求文档标准结构（11 章节）

需求文档必须包含以下 11 个章节，章节编号必须连续且正确：

```
1. 概述（1.1 背景、1.2 目标、1.3 范围）
2. 角色与用例（用例图、角色说明表）
3. 业务流程（3.1-3.3 核心流程，至少 3 个活动图）
4. 状态说明（4.1 状态机图、4.2 状态说明表）
5. 现有功能详述（5.1 接口清单、5.2 核心方法清单、5.3 业务规则详述）
6. 现有逻辑不足分析（6.1 P0、6.2 P1、6.3 P2、6.4 P3 级缺陷）
7. 市面主流系统对标（7.1 功能对比矩阵、7.2 差距总结，对标 3-4 个竞品）
8. 跨模块缺陷（X-1、X-2...）
9. 验收标准（9.1 现有功能验收、9.2 待修复验收）
10. 演进建议与待办（10.1-10.3 分阶段）
11. 非功能需求（11.1 性能、11.2 可靠性、11.3 安全性、11.4 可维护性）
```

### 5.2 设计文档标准结构（14 章节）

设计文档必须包含以下 14 个章节：

```
1. 概述（1.1 设计目标、1.2 约束条件、1.3 技术选型）
2. 架构与模块（组件图、模块职责说明）
3. 领域/数据模型（类图、实体关系说明）
4. 核心流程时序（4.1-4.3 至少 3 个时序图）
5. 状态与流程（5.1 状态机实现、5.2 流程控制）
6. 部署架构（部署图、部署说明）
7. 缺陷改进方案（7.1 P0、7.2 P1、7.3 P2 级修复方案）
8. 架构改进方案
9. 接口设计（9.1 HTTP、9.2 内部、9.3 事件）
10. 数据库设计（10.1 表结构、10.2 索引、10.3 迁移）
11. 缓存设计（11.1 策略、11.2 更新、11.3 失效）
12. 安全设计（12.1 认证授权、12.2 加密、12.3 审计）
13. 性能优化（13.1 查询、13.2 并发、13.3 资源）
14. 测试策略（14.1 单元、14.2 集成、14.3 性能）
```

---

## 6. 图的表达规范

- **图须有标题**：置于图上方或下方，格式如「图 1：XXX 用例图」。
- **图须有说明**：图中关键元素在正文中解释，不依赖「读图自明」。
- **与正文一致**：图中命名、关系须与文字描述一致，禁止图文不符。

---

## 7. 触发与适用

- **何时遵循**：用户要求生成需求文档、设计文档，或对 `docs/**`、`**/docs/**` 下的文档进行编写/补充时。
- **可省略场景**：纯修复、小改动、内部备忘类文档；若用户明确不需完整文档，可简化执行。

---

## 8. 文件命名规范

- **强制小写**：`docs/` 及 `apps/backend/docs/` 下新建文档文件名必须全部使用小写字母，单词之间用连字符 `-` 分隔。
  - ✅ `payment-service-design.md`、`commission-requirements.md`
  - ❌ `PAYMENT_SERVICE_DESIGN.md`、`Commission_Requirements.md`
- **目录名**：同样使用小写 + 连字符，如 `docs/archive/`、`docs/e2e-tests/`。

---

## 9. 文档目录归类规范

### 9.1 文档位置选择

| 文档层级       | 位置               | 适用范围                               |
| -------------- | ------------------ | -------------------------------------- |
| **项目级文档** | `docs/`            | 整个项目的通用文档、用户指南、部署指南 |
| **应用级文档** | `apps/{app}/docs/` | 特定应用的技术文档、需求文档、设计文档 |

**选择原则**：后端模块文档放 `apps/backend/docs/`；前端功能文档放 `apps/admin-web/docs/`；项目通用指南放 `docs/`。

### 9.2 后端文档目录结构（apps/backend/docs/）

| 文档类型 | 目录                                | 示例                                                         |
| -------- | ----------------------------------- | ------------------------------------------------------------ |
| 需求文档 | `apps/backend/docs/requirements/`   | `requirements/finance/commission/commission-requirements.md` |
| 设计文档 | `apps/backend/docs/design/`         | `design/finance/commission/commission-design.md`             |
| 指南文档 | `apps/backend/docs/guides/`         | `guides/quick-start.md`                                      |
| 任务文档 | `apps/backend/docs/tasks/`          | `tasks/architecture-optimization.md`                         |
| 计划文档 | `apps/backend/docs/plans/`          | `plans/performance-optimization-plan.md`                     |
| 改进文档 | `apps/backend/docs/improvements/`   | `improvements/p1-eliminate-finance-any-types.md`             |
| 重构文档 | `apps/backend/docs/refactoring/`    | `refactoring/commission-service-refactoring-summary.md`      |
| 总结文档 | `apps/backend/docs/summaries/`      | `summaries/performance-optimization-summary.md`              |
| 参考文档 | `apps/backend/docs/references/`     | `references/api-reference.md`                                |
| 最佳实践 | `apps/backend/docs/best-practices/` | `best-practices/logging-best-practices.md`                   |
| 测试文档 | `apps/backend/docs/testing/`        | `testing/e2e-test-guide.md`                                  |
| 归档文档 | `apps/backend/docs/archive/`        | `archive/marketing-reset-guide.md`                           |

**模块对齐**：文档路径与 `src/module/` 同构，如 `src/module/finance/commission/` → `docs/requirements/finance/commission/`。

### 9.3 项目级文档目录（docs/）

| 文档类型     | 目录                  |
| ------------ | --------------------- |
| 用户指南     | `docs/guide/`         |
| 开发文档     | `docs/development/`   |
| 架构设计     | `docs/design/`        |
| 部署文档     | `docs/deployment/`    |
| 在线部署步骤 | `docs/deploy-online/` |
| 功能说明     | `docs/features/`      |

---

## 10. 缺陷分析前置检查规范

编写「缺陷分析」章节时，**必须先查阅项目实际代码**，禁止凭模块内部代码推断项目全局结构。

### 10.1 跨模块文档查阅规范（必须遵守）

**查阅范围**：

1. **同级模块文档**：`docs/requirements/{能力域}/` 下其他子模块的整体分析文档和需求文档
2. **关联模块文档**：可能存在功能交叉的其他能力域（如 Finance 与 Marketing、Store 与 PMS）
3. **C 端接口文档**：`src/module/client/{能力域}/` 下的 C 端接口实现

**常见误判**：

| 误判场景                  | 正确做法                                                 |
| ------------------------- | -------------------------------------------------------- |
| 认为 Finance 缺少积分系统 | 查阅 Marketing 模块，积分在 `marketing/points/` 中实现   |
| 认为 Finance 缺少优惠券   | 查阅 Marketing 模块，优惠券在 `marketing/coupon/` 中实现 |
| 认为模块缺少 C 端接口     | 查阅 `src/module/client/{能力域}/` 目录                  |

### 10.2 C 端接口缺陷检查

分析某能力域是否缺少 C 端接口时，必须先检查 `src/module/client/` 目录。C 端接口统一放在 `module/client/{能力域}/` 下，能力域模块内看不到 `client/*` Controller 是正常的。

### 10.3 跨模块依赖检查

分析跨模块缺陷时，必须确认：被引用的 Service/Repository 是否已通过 Module exports 正确导出；调用方是否通过 Module imports 正确引入。

### 10.4 Mermaid 图表编码规范

- 禁止使用 `→`、`←`、`≤`、`≥`、`∈` 等 Unicode 特殊符号，改用纯 ASCII 文字描述
- 禁止在节点标签中使用 `<br/>`，改用空格或换行
- 节点标签中避免使用 `<`、`>` 符号，改用「大于」「小于」等文字

---

## 11. 文档编写最佳实践

### 11.1 分段写入策略（必须遵守）

编写长文档（>50 行）时，必须使用分段写入策略：

- fsWrite 单次写入限制：50 行
- 超过 50 行必须使用 fsWrite + fsAppend 组合
- 第一段：核心章节框架（<50 行）；后续每段建议 <100 行

### 11.2 工具使用规范

**批量读取优先**：一次读取多个文件，减少工具调用。`readMultipleFiles(['controller.ts', 'service.ts', 'repository.ts', 'dto/*.ts', 'module.ts'])`。

**字符串替换**：`strReplace` 前必须先 `readFile` 确认 oldStr 完全匹配；优先用 `fsAppend` 追加内容。

**常见错误**：fsWrite 超 50 行会失败 → 改用分段写入；strReplace 不匹配会失败 → 先读取确认或改用 fsAppend。

### 11.3 批量文档编写流程

**准备**：确认模块列表、文档类型；第一个模块建立标准模板。

**单个模块流程**：探索模块结构 → 批量读取源码 → 分析业务逻辑 → 创建需求文档（分段写入）→ 创建设计文档（分段写入）→ 识别缺陷并 P0-P3 分级。

**第一个模块**（建立标准，约 2 小时）：详细阅读代码；完整编写需求+设计文档；建立模板。

**后续模块**（复用标准，约 30-45 分钟）：快速查阅相关文档；复用文档结构；仅修改业务内容。批量处理时每 5-10 个模块验收一次。

**提示词**：首次用详细指令建立标准；后续用「继续」保持效率；避免每个模块重复详细描述。

### 11.4 文档质量检查清单

**结构完整性**：包含所有必需章节、图表；章节编号正确且连续。

**内容准确性**：术语统一；数据准确（接口路径、表名、字段名）；逻辑严谨；图文一致。

**规范遵循**：文件名小写+连字符；目录归类正确；Mermaid 使用纯 ASCII；缺陷分析有 P0-P3 分级。

**缺陷分析**：已查阅相关模块文档；已查阅 client/ 目录；对照代码检查；明确模块职责边界。

### 11.5 缺陷分析深度要求

每个缺陷必须包含**现状、影响、建议**。按 P0-P3 分级：

- **P0**：阻塞性（认证失败、数据丢失、安全漏洞、租户隔离缺失）
- **P1**：高优先级（N+1 查询、功能缺失、God Class）
- **P2**：中优先级（功能不完善、扩展性不足）
- **P3**：低优先级（优化建议、技术债务）

---

## 12. 大模块文档编写流程（先整体后子模块）

当需要开发包含多个子模块的大模块时（如 finance、marketing、store），必须遵循「先整体后子模块」：

### 12.1 第一步：整体架构评估文档

**文档名称**：`{module}-overall-analysis.md`
**存放位置**：`apps/backend/docs/requirements/{module}/{module}-overall-analysis.md`

**文档结构**（10 章节）：概述、主流平台对标、业务场景覆盖度、缺失模块分析（P0-P3）、架构完整性、演进路线图、资源评估、风险与挑战、总结与建议、附录。

### 12.2 第二步：子模块需求文档

在完成整体架构评估后，按优先级编写子模块需求文档。存放位置：`apps/backend/docs/requirements/{module}/{submodule}/{submodule}-requirements.md`。

### 12.3 第三步：子模块设计文档

存放位置：`apps/backend/docs/design/{module}/{submodule}/{submodule}-design.md`。
