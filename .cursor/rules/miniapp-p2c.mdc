---
description: P2C 需求文档到逻辑代码的解析与生成策略（借鉴去哪儿 PRD 结构化解析 + 多 Agent 并发）
globs: apps/miniapp-client/**/*.vue,apps/miniapp-client/**/*.ts
alwaysApply: false
---

# P2C 需求文档到代码生成规则

当根据 PRD（需求文档）生成逻辑代码时，遵循以下工作流。
核心理念：**先将 PRD 结构化，再分步注入到已有 UI 代码中**。

---

## 1. PRD 结构化格式

### 1.1 标准结构（参考去哪儿 PRD 解析方案）

无论 PRD 来源是 Notion、Markdown 还是粘贴文本，AI 应先将其解析为以下结构化格式再进行代码生成：

```typescript
interface StructuredPRD {
  /** 需求标题 */
  title: string
  /** 需求背景：保留 Markdown 格式 */
  background: string
  /** 功能目标 */
  objectives: string[]
  /** 详情表格：按业务模块分组 */
  detail: PRDModule[]
  /** 非功能需求 */
  nonFunctional?: string[]
  /** 验收标准 */
  acceptanceCriteria?: string[]
}

interface PRDModule {
  /** 模块名称（如"活动主流程"、"关卡系统"） */
  moduleName: string
  /** 模块内的场景列表 */
  scenarios: PRDScenario[]
}

interface PRDScenario {
  /** 场景/状态名称 */
  scene: string
  /** 设计稿/demo 图片 URL（如有） */
  demoImages: string[]
  /** 功能说明（保留 Markdown 格式的列表、加粗等） */
  description: string
  /** 涉及的 API 接口（如已知） */
  apis?: string[]
  /** 涉及的状态/Store（如已知） */
  stores?: string[]
}
```

### 1.2 解析示例

用户提供的 PRD（如下原始 JSON 或文字描述）：
```json
{
  "title": "春运产品需求文档",
  "background": "为充分承接春运流量红利...",
  "detail": [
    [
      ["场景\n **进入活动页-未登录**", "demo\n ![image](url)", "说明\n APP弹窗需更新背景图"],
      ["场景\n **网络异常/请求异常**", "demo\n ![image](url)", "说明\n 1.主按钮显示\"剩余？次\"\n2.点击时未登录弹登录弹窗\n3.网络异常提示\"小驼开小差了\""]
    ]
  ]
}
```

应解析为：
```typescript
const prd: StructuredPRD = {
  title: '春运产品需求文档',
  background: '为充分承接春运流量红利...',
  objectives: ['承接春运流量', '提升销售与用户活跃度'],
  detail: [
    {
      moduleName: '活动主流程',
      scenarios: [
        {
          scene: '进入活动页-未登录',
          demoImages: ['https://...xxx.png'],
          description: 'APP弹窗需更新背景图',
          apis: [],
          stores: ['auth'],
        },
        {
          scene: '网络异常/请求异常',
          demoImages: ['https://...xxx.png'],
          description: '1. 主按钮显示"剩余？次"\n2. 点击时未登录弹登录弹窗\n3. 网络异常提示"小驼开小差了，请稍后重试"',
          apis: ['活动接口'],
          stores: ['auth'],
        },
      ],
    },
  ],
}
```

---

## 2. P2C 工作流（三步走）

### 第一步：PRD 结构化

**输入**：用户粘贴的 PRD 文本 / JSON / Notion 链接
**输出**：`StructuredPRD` 格式

操作：
1. 识别文档中的**表格**，将每个表格解析为一个 `PRDModule`
2. 表格的每一行解析为一个 `PRDScenario`
3. 保留 Markdown 格式（加粗、列表、图片链接）
4. 识别隐含的 API 调用和状态管理需求
5. 向用户确认解析结果是否正确

### 第二步：模块级路由（复杂 PRD 时）

当 PRD 包含 **3 个以上模块** 或 **总字符数超过 5000** 时，按模块分片处理：

```
PRD → 模块A（活动主流程）→ 生成 activity-flow 相关代码
    → 模块B（关卡系统）  → 生成 checkpoint 相关代码
    → 模块C（奖品系统）  → 生成 reward 相关代码
```

每个模块独立生成，最后在页面级组件中组装。

### 第三步：逻辑代码注入

基于已有的 UI 代码（D2C 生成或手写），逐模块注入逻辑：

1. **API 接口对接**
   - 在 `src/api/` 下创建或复用接口定义
   - 使用 `@libs/common-types` 的类型
   - 接口请求/响应必须有 TypeScript 类型

2. **状态管理**
   - 在 `src/store/` 下创建或复用 Pinia store
   - 按业务实体组织（不按页面）

3. **交互逻辑**
   - 条件渲染（登录态、网络状态、空状态）
   - 跳转（`uni.navigateTo` / `uni.switchTab`）
   - 事件处理（点击、表单提交）

4. **异常处理**
   - 网络错误：提示文案 + 重试按钮
   - 业务错误：根据 PRD 说明展示对应提示
   - 未登录：弹出登录弹窗（`src/components/global-auth-modal/`）

---

## 3. 场景到代码的映射规则

### 3.1 常见场景映射

| PRD 中的描述 | 代码实现 |
|-------------|---------|
| "未登录弹窗" / "需要登录" | 调用 `src/store/auth.ts` 检查登录态 → 弹出 `GlobalAuthModal` |
| "网络异常提示" / "请求失败" | try-catch + `uni.showToast({ title: '提示文案', icon: 'none' })` |
| "点击跳转到 XX 页面" | `uni.navigateTo({ url: '/pages/xx/xx' })` |
| "加入购物车" | 调用 `src/store/cart.ts` 的 `addToCart` action |
| "下拉刷新" / "上拉加载" | `<z-paging>` 组件 + API 分页 |
| "倒计时" / "定时" | `setInterval` + `onUnmounted` 清理 |
| "分享" | `onShareAppMessage` / `onShareTimeline`（uni 生命周期） |
| "支付" | 调用 `src/api/payment.ts` → `uni.requestPayment` |
| "显示/隐藏某模块" | `v-if` / `v-show` + 响应式变量 |
| "列表为空时显示 XX" | `<wd-status-tip>` 空状态组件 |
| "按钮显示'剩余N次'" | 动态绑定 `:label="'剩余' + count + '次'"` |
| "弹窗确认" | `<wd-message-box>` 或 `uni.showModal` |

### 3.2 条件分支处理

PRD 中的"如果...则..."映射为代码：

```vue
<script setup lang="ts">
import { useAuthStore } from '@/store/auth'

const authStore = useAuthStore()

function handleAction() {
  if (!authStore.isLoggedIn) {
    // PRD: "如果未登录弹窗登录弹窗"
    authStore.showLoginModal()
    return
  }
  // PRD: "如果网络异常提示 xxx"
  try {
    await doAction()
  }
  catch {
    uni.showToast({ title: '小驼开小差了，请稍后重试', icon: 'none' })
  }
}
</script>
```

---

## 4. Notion 集成（可选）

如果 PRD 在 Notion 的 Requirements 表中：

1. 使用 Notion MCP 的 `database-query` 查询需求详情
2. 读取需求页面内容（背景、目标、功能说明等）
3. 使用 `spec-to-implementation` 将 PRD 拆解为 Tasks
4. 按 Task 逐个生成代码

### Notion Requirements 页面模板对应关系

| PRD 模板字段 | 对应 StructuredPRD 字段 |
|-------------|----------------------|
| 背景 | `background` |
| 目标 | `objectives` |
| 功能说明 | `detail`（需进一步解析为模块和场景） |
| 非功能要求 | `nonFunctional` |
| 验收标准 | `acceptanceCriteria` |

---

## 5. 质量检查

生成逻辑代码后，自查以下项：

- [ ] 所有 PRD 中描述的场景都有对应代码实现
- [ ] 条件分支完整（登录态、网络异常、空数据、正常态）
- [ ] API 调用使用了正确的接口（`src/api/` 中已有的）
- [ ] 错误提示文案与 PRD 一致
- [ ] 使用了项目已有的组件和工具（不重复造轮子）
- [ ] 类型安全（无 `any`，请求/响应有类型）
- [ ] 需要租户的接口正确传递了 `tenant-id`
- [ ] 页面跳转路径正确（与 `pages.config.ts` 一致）

---

## 6. 禁止事项

- **禁止** 跳过 PRD 结构化直接生成代码（容易遗漏场景）
- **禁止** 凭空编造 API 接口名和参数（必须查看 `src/api/` 或 `@libs/common-types`）
- **禁止** 忽略 PRD 中的异常场景（网络错误、未登录等）
- **禁止** 硬编码提示文案（如果 PRD 指定了文案，必须使用原文）
- **禁止** 在逻辑注入时破坏已有 UI 结构
