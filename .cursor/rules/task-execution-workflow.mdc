---
description: 任务执行工作流规范（架构审查、任务清单、逐个执行、测试驱动、批量总结）
globs: docs/tasks/**,docs/requirements/**,docs/improvements/**
alwaysApply: false
---

# 任务执行工作流规范

> 当用户提供需求分析文档后，自动触发此工作流

## 触发条件

当用户提供以下类型的文档时，自动应用此工作流：

- 需求分析文档（requirements.md）
- 缺陷分析文档（defects.md）
- 改进计划文档（improvements.md）
- 包含待办任务列表的文档

## 工作流步骤

### 第 0 步：架构审查（Architecture Review）⭐ 新增

> 需求文档可能存在遗漏、过时、或未充分考虑架构影响的问题。
> 在提取任务之前，必须先用 `architecture-meta-model.md` 的四棱镜对需求文档进行审查。

#### 0.1 相态判断

确认需求涉及的模块当前处于什么相态（参照 `architecture-meta-model.md §0`）：

- 查阅 §0.6 的相态判断表，确认模块相态是否仍然准确
- 如果需求文档的编写时间距今 > 1 个月，或模块发生了重大变化，触发相态重评估
- 输出：`当前相态：探索态 / 成长态 / 运营态`，以及对应的四棱镜权重

#### 0.2 四棱镜快速扫描

对需求文档中的每个主要任务/改动，快速过一遍四棱镜，识别潜在问题：

| 棱镜         | 审查问题                                                       | 红旗信号 🚩                                    |
| ------------ | -------------------------------------------------------------- | ---------------------------------------------- |
| 业务价值棱镜 | 这个需求对业务的直接价值是什么？有没有"技术自嗨"型任务？       | 无法说清业务价值的任务                         |
| 时间棱镜     | 需求文档中的优先级排序是否合理？短期任务的收益是否覆盖成本？   | 短期任务中混入了"锦上添花"型需求               |
| 能力棱镜     | 任务是否超出团队当前能力？是否存在单点依赖风险？               | 某个任务只有一个人能做，且没有降级方案         |
| 复杂性棱镜   | 任务是否会引入新的跨模块依赖？退出成本是否被评估？             | 新增外部依赖但未评估退出成本                   |

#### 0.3 需求补全检查

检查需求文档是否遗漏了以下关键维度：

- [ ] 模块边界是否清晰？（§2.1：核心职责 3 句话，排除项 5 句话）
- [ ] 新增的跨模块连接是否被识别？（§2.2：连接比组件更贵）
- [ ] 是否考虑了对现有抽象层的影响？（§2.3）
- [ ] 是否预设了上线后的风险信号？（§3）
- [ ] 涉及 AI 相关功能时，是否考虑了约束波动性？（§1.5）

#### 0.4 审查输出

审查结果记录在任务清单文档的头部（见第 1 步的文件格式），分为三种结论：

| 结论       | 含义                                         | 后续动作                                   |
| ---------- | -------------------------------------------- | ------------------------------------------ |
| ✅ 通过    | 需求文档充分，可直接提取任务                 | 进入第 1 步                                |
| ⚠️ 补充   | 需求文档有遗漏但不影响执行方向，边做边补     | 在任务清单中标注需要补充的点，进入第 1 步  |
| 🛑 重审    | 需求文档存在方向性问题，需要先修正再执行     | 输出问题清单，与用户确认后再进入第 1 步    |

**⚠️ 补充** 的典型场景：
- 需求文档缺少对跨模块影响的分析 → 在任务清单中补充"前置任务：确认 XX 模块的接口契约"
- 某个任务的工时估算明显偏低 → 在任务清单中调整工时并标注原因
- 缺少风险信号预设 → 在任务清单末尾补充"上线后监控项"

**🛑 重审** 的典型场景：
- 需求文档中的任务与当前相态不匹配（如：运营态模块被要求做探索态级别的大改）
- 业务价值不明确，无法回答"为什么要做这个"
- 任务之间存在循环依赖或顺序矛盾
- 需求文档基于的前提假设已经过时（如：依赖的上游接口已变更）

---

### 第 1 步：创建任务清单

从需求文档中提取任务，创建简洁的任务清单文档：

**文件位置**：`docs/tasks/{模块名}-task-list.md`

**文件格式**：

```markdown
# {模块名}任务清单

> 来源：{需求文档路径}
> 创建时间：{日期}
> 架构审查：✅ 通过 / ⚠️ 补充（见下方备注）/ 🛑 重审（已修正）
> 模块相态：{探索态 / 成长态 / 运营态}

## 架构审查备注（仅 ⚠️ 补充 时填写）

- 补充点 1：...
- 补充点 2：...
- 上线后监控项：...

## 短期任务（1-2周）

- [ ] T-1: 任务名称 (预估工时)
- [ ] T-2: 任务名称 (预估工时)
- [ ] T-3: 任务名称 (预估工时)

## 中期任务（1-2月）

- [ ] T-4: 任务名称 (预估工时)
- [ ] T-5: 任务名称 (预估工时)

## 长期任务（3-6月）

- [ ] T-6: 任务名称 (预估工时)
```

**要求**：

- ✅ 简洁明了，只包含任务编号、名称、预估工时
- ✅ 使用 checkbox `- [ ]` 标记未完成，`- [x]` 标记已完成
- ✅ 按优先级/时间分组
- ✅ 头部包含架构审查结论和模块相态
- ❌ 不包含详细描述、背景、分析（这些在需求文档中）

### 第 2 步：逐个执行任务

每完成一个任务，必须遵循以下标准流程：

#### 2.1 实现功能代码

- 遵循项目规范（backend-nestjs.md / admin-web-frontend.md）
- 使用类型安全、异常处理、事务等最佳实践
- 代码简洁，单函数不超过 80 行
- 探索态代码必须标注 `// EXPLORATION: 此实现为快速验证，PMF 确认后需重构`

#### 2.2 编写单元测试 ⭐ 必须

- 测试文件位置：与源文件同目录，命名为 `*.spec.ts`
- 测试覆盖：
  - ✅ 正常情况（Happy Path）
  - ✅ 边界情况（Edge Cases）
  - ✅ 异常情况（Error Cases）
- 测试文件顶部添加 `// @ts-nocheck`

**测试模板**：

```typescript
// @ts-nocheck
/* eslint-disable @typescript-eslint/no-explicit-any */
import { Test, TestingModule } from '@nestjs/testing';

describe('ServiceName', () => {
  let service: ServiceName;

  beforeEach(async () => {
    const module: TestingModule = await Test.createTestingModule({
      providers: [
        ServiceName,
        // mock dependencies
      ],
    }).compile();

    service = module.get<ServiceName>(ServiceName);
  });

  describe('methodName', () => {
    it('应该成功处理正常情况', async () => {
      // Arrange
      // Act
      // Assert
    });

    it('应该在边界情况下正确处理', async () => {
      // Test edge cases
    });

    it('应该在异常情况下抛出错误', async () => {
      // Test error cases
    });
  });
});
```

#### 2.3 运行测试

```bash
# 运行单个测试文件
pnpm --filter backend test -- service-name.spec

# 运行所有测试
pnpm --filter backend test
```

#### 2.4 更新任务清单

- 将 `- [ ]` 改为 `- [x]`
- 可选：添加完成时间和备注

```markdown
- [x] T-1: SKU 创建时传入 costPrice (0.5h) ✅ 2026-02-26
```

### 第 3 步：批量完成后总结

当一批任务完成后（如短期任务全部完成），创建总结文档：

**文件位置**：`docs/improvements/{模块名}-{批次}-summary.md`

**内容包含**：

- 已完成任务列表
- 测试结果统计
- 规范合规验证
- 架构审查中标注的补充点是否已落实
- 遗留问题（如有）
- 上线后需要监控的风险信号（来自第 0 步的预设）

---

## 工作流原则

### ✅ 必须做

1. **架构审查先行**：收到需求文档后，第一件事是用四棱镜审查，不是直接提取任务
2. **任务清单次之**：审查通过后，创建任务清单
3. **测试驱动**：每个任务必须有对应的单元测试
4. **逐个完成**：一次只专注一个任务，完成后再进行下一个
5. **及时标记**：完成任务后立即更新 checkbox
6. **保持简洁**：任务清单只记录任务，不重复需求分析

### ❌ 不要做

1. **不要跳过架构审查**：即使需求文档看起来很完善，也要过一遍四棱镜
2. **不要跳过测试**：即使是简单的修复，也要写测试
3. **不要批量标记**：完成一个标记一个，不要事后补标记
4. **不要在任务清单中写详细描述**：详细内容在需求文档中
5. **不要盲目信任需求文档的优先级**：用业务价值棱镜重新校验

---

## 工作流全景图

```
需求文档进来
    ↓
第 0 步：架构审查（四棱镜扫描）
    ├── ✅ 通过 → 第 1 步
    ├── ⚠️ 补充 → 标注补充点 → 第 1 步
    └── 🛑 重审 → 输出问题清单 → 与用户确认 → 修正后重新进入第 0 步
    ↓
第 1 步：创建任务清单（含审查结论 + 模块相态）
    ↓
第 2 步：逐个执行任务
    ├── 2.1 实现功能代码
    ├── 2.2 编写单元测试
    ├── 2.3 运行测试
    └── 2.4 更新任务清单
    ↓
第 3 步：批量完成后总结（含风险信号监控项）
```

---

## 示例

### 输入：需求文档

```markdown
# PMS 模块需求分析

## 背景
...（大量背景描述）

## 缺陷分析
- D-1: 无商品删除接口
- D-2: 无独立上下架接口
...

## 待办任务

### 短期（1-2周）
- T-1: SKU 创建时传入 costPrice (0.5h)
- T-2: 新增独立上下架接口 (1h)
...
```

### 第 0 步输出：架构审查

```markdown
## 架构审查：PMS 模块需求

**模块相态**：成长态（需求确定性 ~70%，月变更 5-6 次）

**四棱镜扫描**：
- 业务价值：✅ 所有任务都有明确的业务价值（商品管理是核心链路）
- 时间棱镜：⚠️ T-10（商品变更事件机制）工时估算偏低，事件机制涉及跨模块影响，建议 3-5d
- 能力棱镜：✅ 团队熟悉 PMS 模块，无单点风险
- 复杂性棱镜：⚠️ T-8（商品删除接口）需要考虑关联数据（SKU、库存、订单），需求文档未分析级联影响

**审查结论**：⚠️ 补充
- 补充点 1：T-8 需要先确认删除策略（软删除 vs 硬删除）以及对 SKU/库存/订单的级联影响
- 补充点 2：T-10 的工时调整为 3-5d，需要定义事件契约（EventName + Payload 类型）
- 上线后监控项：商品删除后 7 天内关联查询的 404 率
```

### 第 1 步输出：任务清单

```markdown
# PMS 模块任务清单

> 来源：docs/requirements/pms/pms-requirements.md
> 创建时间：2026-02-26
> 架构审查：⚠️ 补充（见下方备注）
> 模块相态：成长态

## 架构审查备注

- T-8 需要先确认删除策略（软删除 vs 硬删除）以及对 SKU/库存/订单的级联影响
- T-10 工时调整为 3-5d，需要定义事件契约
- 上线后监控项：商品删除后 7 天内关联查询的 404 率

## 短期任务（1-2周）

- [ ] T-1: SKU 创建时传入 costPrice (0.5h)
- [ ] T-2: 新增独立上下架接口 (1h)
- [ ] T-3: 上下架调用通知方法 (1h)
- [ ] T-4: 注册 Bull 队列 (1h)
- [ ] T-5: 创建 UpdateProductDto (0.5h)
- [ ] T-6: 商品列表价格改为 MIN (0.5h)
- [ ] T-7: 添加 @ApiBearerAuth (0.5h)

## 中期任务（1-2月）

- [ ] T-8: 商品删除接口 — 前置：确认删除策略 (2-3d)
- [ ] T-9: 校验 distRate 范围 (1d)
- [ ] T-10: 商品变更事件机制 — 含事件契约定义 (3-5d) ⬆️ 工时已调整
```

---

## 文件组织

```
docs/
├── requirements/          # 需求文档（详细分析）
│   └── pms/
│       └── pms-requirements.md
├── tasks/                 # 任务清单（简洁执行，含架构审查结论）
│   └── pms-task-list.md
└── improvements/          # 完成总结
    └── pms-short-term-fixes-summary.md
```

## 检查清单

在开始执行任务前，确认：

- [ ] 已完成架构审查（第 0 步）
- [ ] 审查结论已记录在任务清单头部
- [ ] 如果是 ⚠️ 补充，补充点已标注在对应任务旁
- [ ] 如果是 🛑 重审，已与用户确认并修正
- [ ] 已创建任务清单文档
- [ ] 任务清单简洁明了（无冗余描述）
- [ ] 任务按优先级分组
- [ ] 每个任务有预估工时

在完成每个任务后，确认：

- [ ] 功能代码已实现
- [ ] 单元测试已编写（覆盖正常/边界/异常）
- [ ] 测试已运行并通过
- [ ] 任务清单已更新（checkbox 标记为完成）

在批量任务完成后，确认：

- [ ] 所有任务的 checkbox 已标记
- [ ] 已创建完成总结文档
- [ ] 测试统计已记录
- [ ] 架构审查中的补充点已全部落实
- [ ] 上线后监控项已交付给相关 Owner

---

**版本**：2.0
**最后更新**：2026-02-27
**适用范围**：所有需求驱动的开发任务
