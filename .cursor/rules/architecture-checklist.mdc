---
description: 架构决策检查清单——PR可直接使用的模板（触发判断、分级、轻量/完整模板、方案对比）
globs: docs/design/**,docs/requirements/**,apps/backend/docs/**,apps/admin-web/docs/**,apps/miniapp-client/docs/**
alwaysApply: false
---

# 架构决策 Checklist（执行检查层）

> PR 提交、Code Review、上线前检查时使用。直接复制模板填写。
> 原则依据 → 见 `architecture-meta-model.mdc`
> 项目相态与信号 → 见 `architecture-playbook.mdc`

---

## 0. 快速入口：30 秒判断要不要走决策流程

回答以下问题，任一为"是"则需要走决策流程：

- [ ] 是否新增或替换了外部依赖？
- [ ] 是否新增了跨模块依赖？
- [ ] 是否变更了数据模型（表结构、字段类型、关联关系）？
- [ ] 是否新增了应用或共享库？
- [ ] 是否涉及技术选型（框架、协议、存储引擎）？
- [ ] 是否做了架构重构（模块拆分/合并、分层调整）？
- [ ] 是否影响了多个应用？

**全部为"否"**：正常开发流程，无需额外决策分析。
**任一为"是"**：根据下方分级选择对应模板。

---

## 1. 决策分级

| 级别 | 特征                             | 使用模板          | 审批要求          | 耗时预估   |
| ---- | -------------------------------- | ----------------- | ----------------- | ---------- |
| L1   | 单模块内部，不新增外部依赖       | 轻量版            | 模块 Owner 自决   | 5 分钟     |
| L2   | 新增跨模块依赖，或引入新工具库   | 轻量版            | 技术负责人 Review | 15 分钟    |
| L3   | 技术选型、架构重构、新增外部服务 | 完整版            | 技术负责人 + TL   | 30-60 分钟 |
| L4   | 影响多个应用                     | 完整版 + 方案对比 | 架构评审会        | 1-2 小时   |

---

## 2. 轻量版模板（L1 / L2）

直接在 PR 描述中填写：

```markdown
### 架构决策：[一句话简述]

1. **相态**：[模块名] 处于 [探索态/成长态/运营态]
2. **业务价值**：[这个改动对用户/业务的直接影响]
3. **最大风险**：[最可能出错的地方]
4. **退出成本**：[如果决策错误，回退需要多少成本]
5. **连接影响**：[是否新增跨模块依赖？方向是否符合架构图？]
```

---

## 3. 完整版模板（L3 / L4）

在以下位置创建独立文档，或在 PR 描述中展开（按 documentation Skill §9 约定）：
- 项目级/跨应用决策 → `docs/design/`
- 后端模块决策 → `apps/backend/docs/design/{模块}/`
- 前端模块决策 → `apps/admin-web/docs/design/`

```markdown
### 决策：[简述]

**相态判断**：

- 涉及模块所处相态：[探索态 / 成长态 / 运营态]
- 四棱镜权重调整说明：[根据相态，哪个棱镜权重最高]

**约束清单**（决策前先穷举）：

- 时间约束：[截止日期/里程碑]
- 人力约束：[谁来做、投入多少]
- 技术约束：[现有技术栈限制]
- 兼容约束：[需要兼容的已有系统/接口]
- 合规约束：[安全/隐私/审计要求]
- 依赖约束：[上下游交付节奏]

**业务价值棱镜**：

- 价值链位置：[核心链路 / 辅助链路]
- 技术 ROI：投入 [X] 人天，预期 [收益]
- 用户感知：[是 / 否，说明]
- 机会成本：做这件事 = 不做 [什么]

**时间棱镜**：

- 短期收益：[本季度解决什么]
- 中期成本：[1 年后维护成本]
- 长期锁定：[3 年后能否低成本替换]
- 半衰期评估：[这个决策多久后可能过时]

**能力棱镜**：

- 团队掌握周期：[从会用到能排障要多久]
- 单点风险：[是否只有一人精通]
- 降级/退出方案：[如果不行怎么退]

**复杂性棱镜**：

- 显性成本：[服务器/许可/运维]
- 隐性成本：[编译时间/部署复杂度/上手门槛]
- 传染范围：[是否扩散到其他模块]
- 退出成本：[移除需要多少人天]

**结构影响**：

- 新增/变更的模块边界：[列出]
- 新增/变更的连接：[列出]
- 对现有抽象层的影响：[是否需要修改现有接口]

**风险信号监控**：

- 上线后关注的信号：[列出]
- 触发回滚/调整的阈值：[量化]

**可逆性判断**：

- 单向门 / 双向门
- 回退成本：[人天估算]

**验证计划**（L3+ 必填）：

- PoC 范围：[验证什么]
- 试点模块：[哪个模块先用]
- 全量条件：[什么标准下全面推广]
```

---

## 4. 方案对比模板（L3+ 有多个候选方案时使用）

```markdown
### 方案对比：[决策主题]

| 维度                           | 方案 A：[名称] | 方案 B：[名称] | 方案 C：[名称] |
| ------------------------------ | -------------- | -------------- | -------------- |
| 一句话描述                     |                |                |                |
| **业务价值**                   |                |                |                |
| **时间成本**（短期/中期/长期） |                |                |                |
| **团队能力匹配度**             |                |                |                |
| **复杂性注入**                 |                |                |                |
| **退出成本**                   |                |                |                |
| **约束满足度**                 |                |                |                |

**推荐方案**：[方案 X]
**推荐理由**：[基于哪个棱镜的判断，以及冲突裁决过程]
**被否决方案的原因**：[简述]
```

---

## 5. PR 自检清单

提交 PR 前，对照以下清单检查：

### 5.1 所有 PR 必检

- [ ] 代码符合项目规范（lint + typecheck 通过）
- [ ] 有对应的测试覆盖
- [ ] 内部包引用使用 `workspace:*`

### 5.2 涉及架构决策的 PR 额外检查

- [ ] 已判断决策级别（L1-L4）
- [ ] 已填写对应模板（轻量版 / 完整版）
- [ ] 跨模块依赖方向符合架构图
- [ ] 新增外部依赖已评估退出成本
- [ ] L3+ 决策有验证计划（PoC → 试点 → 全量）
- [ ] 上线后需要监控的风险信号已明确

### 5.3 数据模型变更的 PR 额外检查

- [ ] 已确认字段类型、默认值、索引
- [ ] 已评估对现有数据的迁移方案
- [ ] 已确认级联影响（关联查询、关联删除）
- [ ] 已确认对前端类型的影响（需要重新生成 `@libs/common-types`）
